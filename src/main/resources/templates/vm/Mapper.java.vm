package ${package}.common.mapper.businessFunction;

import ${package}.mapper.Mapper;
import ${package}.common.model.businessFunction.${className};
import org.springframework.stereotype.Repository;

/**
 * ${comments}-数据访问层接口
 * @author ${author} E-mail:${email}
 * @date 创建时间：${createTime}
 * @version 1.0
 * @since 
 */
@Repository
public interface ${className}Mapper extends Mapper<${className}> {

}